# Weave take-home — Engineering Impact Dashboard

**Read `takehome-prompt.md` first.** It is the full assignment text and the authoritative copy.
`takehome-prompt.pdf` is the original print and is truncated at the page break, losing
everything from "What to Submit" onward.

Assessment `asmt_eD1bGr5o2Ea75Z0p0gnhglFW`. Received 2026-09-15, due ~2026-09-18.
Form trouble or extension: kevin@workweave.ai.

## Layout

| path | holds |
|---|---|
| `scripts/` | data fetch + analysis |
| `data/` | fetched PR/review/commit JSON, computed scores |
| `dashboard/` | the single-page `index.html` that gets deployed |
| `docs/` | agent session export |

Deploy: `npx netlify-cli deploy --prod --dir dashboard`

## Timer

The 90 minutes covers data collection, analysis, the impact definition, the dashboard, and
hosting. Repo scaffolding and background reading happened before it and are disclosed in the
submission.

| started | stopped | elapsed |
|---|---|---|
| | | |
