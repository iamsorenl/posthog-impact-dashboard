data/raw/
posthog-repo/
__pycache__/
.playwright-mcp/
*.png
probe*.json
probe*.err
.netlify/
public/

# conversation exports - submission artifacts, never repo content.
# /export writes into the working dir and names the file after the first prompt,
# so match the date-prefixed shape rather than one specific title.
20[0-9][0-9]-[0-9][0-9]-[0-9][0-9]-*.txt
*.jsonl
